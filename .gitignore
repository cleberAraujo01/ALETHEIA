node_modules/
dist/
*.tsbuildinfo
.turbo/
coverage/

# Artefatos de execução — evidências e relatórios nunca entram no repositório.
# Retenção é responsabilidade do storage de evidências (RN-EXE-004).
.aletheia/
*.aletheia-run.json

# Binários soltos no diretório de trabalho não entram por `git add -A`.
# O docs.zip entrou assim uma vez (804aefb) e teve de ser removido.
*.zip
